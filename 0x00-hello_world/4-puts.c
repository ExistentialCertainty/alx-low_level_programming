#include <stdio.h>

/**
 * main - Entry point
 * Return: Always 0 (Success)
 */
int main(void)
{
	char Funfct[] = "\"Programming is like building a multilingual puzzle";

	puts(Funfct);
	return (0);
}
