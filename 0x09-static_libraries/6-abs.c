#include "main.h"

/**
* _abs - prototype
* @n: int to be checked.
* Description: checks each inputed arguments
* and prints the absolute value
* Return: 0 success
*/

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
